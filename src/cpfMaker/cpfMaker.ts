// const generateRandomNumbers =  (range: number, quantity: number, index: number, numbers:string): string => {
//     if(index == quantity - 1){
//         return numbers
//     }
//     numbers += Math.floor((Math.random() * range))
//     index++
//     return generateRandomNumbers(range, quantity, index, numbers)
// }
// generateRandomNumbers(10, 10, 0, '') satisfies string
const numbers = '123456789' satisfies string
Number(numbers.charAt(2)) * (Number(numbers.indexOf('3')) + 1)
// const createFirstCheckNumber = (numbers: string) => {
//     // PRECISO PEGAR CADA UM DOS DIGITOS E MULTIPLICAR PELA POSICAO DELE
// }

